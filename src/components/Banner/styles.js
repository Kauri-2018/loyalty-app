'use strict'

import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  banner: {
    width: 200,
    height: 150
  },
  container: {
    flex: 1,
    flexDirection: 'row'
  }
})
