'use strict'

import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  container: {
    flex: 1,
    paddingTop: 65,
    display: 'flex',
    flexDirection: 'row'
  },
  banner: {
    paddingTop: 20,
    justifyContent: 'center',
    alignItems: 'center'
  },
  logo: {
    alignItems: 'center',
    flex: 1
  }
})
